<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The INTERNET permission is required for development. Specifically,
         the Flutter tool needs it to communicate with the running application
         to allow setting breakpoints, to provide hot reload, etc.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Debug-only: dev backend is plain http://10.0.2.2:5000 (AppConfig.apiBaseUrl),
         and Android blocks cleartext traffic by default for apps targeting API 28+ —
         without this, every API call from an emulator/device fails with a generic
         "connection failed" and no useful error. Release builds never get this override. -->
    <application android:usesCleartextTraffic="true" />
</manifest>
